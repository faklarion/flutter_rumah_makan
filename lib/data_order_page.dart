import 'package:flutter/material.dart';

class DataOrderPage extends StatelessWidget {
  const DataOrderPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Data Order'),
      ),
      body: const Center(
        child: Text('Halaman Data Order'),
      ),
    );
  }
}
