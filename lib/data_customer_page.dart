import 'package:flutter/material.dart';

class DataCustomerPage extends StatelessWidget {
  const DataCustomerPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Data Customer'),
      ),
      body: const Center(
        child: Text('Halaman Data Customer'),
      ),
    );
  }
}
